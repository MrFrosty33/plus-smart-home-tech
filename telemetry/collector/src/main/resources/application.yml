spring:
  config:
    import: classpath:/application-general.yml

logging:
  level:
    root: INFO
    ru.yandex.practicum.collector: TRACE

grpc:
  server:
    port: 59091  # по заданию вроде такой
    enableKeepAlive: true
    keepAliveWithoutCalls: true
    negotiationType: plaintext